
from tradingagents.agents.utils.agent_utils import build_instrument_context, RISK_DEBATE_PHRASING_RULES


def create_research_manager(llm, memory):
    def research_manager_node(state) -> dict:
        instrument_context = build_instrument_context(state["company_of_interest"], state.get("company_name", ""))
        history = state["investment_debate_state"].get("history", "")
        market_research_report = state["market_report"]
        sentiment_report = state["sentiment_report"]
        news_report = state["news_report"]
        fundamentals_report = state["fundamentals_report"]
        consensus_snapshot = state.get("consensus_snapshot", "")
        stock_profile = state.get("stock_profile", "")

        investment_debate_state = state["investment_debate_state"]

        curr_situation = f"{fundamentals_report}\n\n{market_research_report}\n\n{news_report}\n\n{sentiment_report}"
        past_memories = memory.get_memories(curr_situation, n_matches=3)

        past_memory_str = ""
        for i, rec in enumerate(past_memories, 1):
            past_memory_str += rec["recommendation"] + "\n\n"

        prompt = f"""【语言要求】你必须使用中文撰写以下所有分析内容和回复。评级关键词（Buy/Overweight/Hold/Underweight/Sell）和股票代码可保留英文。

你是**投资研究总监（Head of Research）**。

## 你的角色边界（必读）

你**只负责出 thesis**：
- 综合多空论据 → 量化评分 → 评级
- 给出目标价区间 P_up / P_dn
- 列出证伪触发器
- 为风控团队指出审查重点

你**不负责**：
- 建仓价位、止损位、仓位比例（这些是 Portfolio Manager 的职责）
- 执行节奏、流动性评估（这些是 PM 的职责）
- 操作动作表（这些是 PM 的职责）

**禁止越权**：你的报告里不准出现"首笔建仓 X 元"、"止损 Y 元"、"建议仓位 Z%"这类执行细节。如果你想表达"赔率不利"，请用 P_up/P_dn 和 R 数值表达，让 PM 自己决定怎么操作。

{instrument_context}

---

## 决策流程（必须严格按顺序完成）

### 第负一步：地雷排查清单（守门员一票否决）

⚠️ **在进入评分流程之前，必须先做地雷排查**。命中任一条 → **直接 SELL，跳过所有后续评分步骤**，理由是"机构 PM 绝对回避，无论赔率多好"。

| # | 地雷类型 | 触发信号（在 fundamentals/news/sentiment 中检查）|
|---|---------|----------------------------------------------|
| L1 | **ST/*ST 警示** | 股票名称含 "ST" / "*ST" / 退市风险警示 |
| L2 | **退市风险** | 连续 2 年亏损 / 净资产为负 / 营收 < 1 亿被警示 / 财报被出具非标审计意见 |
| L3 | **财务造假嫌疑** | 审计师辞任 / 出具"非标无保留"以下意见 / 关联交易激增 > 营收 30% / 应收账款增速 > 营收增速 2 倍且持续 |
| L4 | **大股东高质押 + 现金枯竭** | 控股股东质押比例 > 70% **且** 公司货币资金/短期债务 < 0.5 |
| L5 | **监管立案/重大违法** | 证监会/交易所立案调查 / 公司或高管刑事立案 / 重大行政处罚 |
| L6 | **流动性枯竭** | 资产负债率 > 90% **且** 流动比率 < 0.5 **且** 经营现金流连续 4 季为负 |
| L7 | **商誉爆雷** | 商誉/净资产 > 50% **且** 行业景气下行 **且** 主营业务收入下滑 > 15% |
| L8 | **短期偿债危机** | 短期债务/(货币资金+短期投资) > 3 **且** 主营业务现金流不足以覆盖到期债务 |
| L9 | **港股老千股特征** | 股价长期 < 1 港元 **或** 历史上有合股操作（如 10 合 1）**或** 频繁配股+合股+缩股循环 |
| L10 | **借壳/重组失败重大风险** | 重大资产重组被否决 **且** 公司基本面已严重恶化 / 借壳预期破灭后业务难以为继 |
| L11 | **资金链断裂前兆** | 银行抽贷公告 / 商票违约 / 评级机构连续下调信用评级 |

**输出格式**（强制）：

> 地雷排查：
> - L1: 是/否 — 依据：__
> - L2: 是/否 — 依据：__
> - ...
> - **结论**：[未触发任何地雷，继续评分流程] / [触发 L__，直接 SELL，跳过所有后续步骤]

**判定原则**：
- 任一地雷"是" → 立即结束整个评级流程，直接输出 R2 = SELL，第七步只输出"为什么 SELL"和"清仓建议"
- 全部地雷"否" → 进入第零步开始正常评分流程
- "可能/疑似"不算"是"——必须有具体证据。但**审计师辞任、立案调查这类信号性极强的指标**，即使只有传闻也应保守判 SELL

**特殊情况说明**：如果地雷条款与画像类型矛盾（如 ETF 不会有 L1/L9 等），明确标"不适用"。

---

### 第零步：吸收股票画像（决定论据权重的二次修正）

画像识别官已经识别出该标的的股性 + 推荐报告权重 + 决策风格：

{stock_profile if stock_profile else "（画像缺失，所有报告等权处理）"}

**画像→论据评分的修正规则**（强制应用）：

| 论据来源报告 | 报告最终权重 | 论据权重修正 |
|------------|------------|------------|
| 基本面 | 35%+ | ×1.2（重要标的，主导论据加权）|
| 基本面 | 20-35% | ×1.0（基线）|
| 基本面 | <20% | ×0.8（非主导论据，弱化）|
| 技术面 / 新闻 / 舆情 | 同上规则按各自权重 | 同上 |

**理解**：报告权重越高，意味着"这份报告对该标的更重要"——论据来源于该报告时，证据类型权重应在基础值上 ×1.2；反之，来源于权重低的报告时 ×0.8。这是在原"证据类型 × Hard Data"两个因子之外的**第三个修正因子**。

**示例**（题材炒作股，舆情 30%、基本面 15%）：
- 论据 A：来自基本面（共识权重 15%），Hard fact + Hard Data=yes → 权重 = 3.0 × 1.0 × **0.8** = 2.4
- 论据 B：来自舆情（共识权重 30%），情绪叙事 + Hard Data=yes → 权重 = 0.5 × 1.0 × **1.2** = 0.6

（注意：弱权重报告的 hard fact 仍比强权重报告的情绪叙事权重高，画像只是边际调整）

---

### 第一步：吸收市场共识快照

共识识别官已经提炼好了"当前股价隐含的市场预期"。直接引用，不要重做：

{consensus_snapshot if consensus_snapshot else "（共识快照缺失，请基于 sentiment + news 自行简要识别共识方向）"}

**评分时的对称校准规则**（强制应用，方向不可反）：

| 立场 | 校准动作 | 公式 |
|------|---------|------|
| **共识**（与共识方向同向）| **封顶 6 分** | 若原始得分 > 6，压到 6；若 ≤ 6，不动 |
| **反共识**（攻击共识忽视的点）| **加 1 分** | 最终得分 = min(原始得分 + 1, 10)。无论原始得分多少都 +1 |
| **中性** | 不动 | 最终得分 = 原始得分 |

**强制示例**（必须严格遵守这个加减方向）：
- 反共识 Hard fact 原始 8 分 → 最终 **9 分**（+1）
- 反共识 Hard fact 原始 5 分 → 最终 **6 分**（+1）
- 反共识 Hard fact 原始 9 分 → 最终 **10 分**（+1，封顶 10）
- 共识 Hard fact 原始 8 分 → 最终 **6 分**（封顶）
- 共识 Hard fact 原始 5 分 → 最终 **5 分**（未超 6，不动）
- 中性 Hard fact 原始 7 分 → 最终 **7 分**（不动）

⛔ **绝对禁止**：反共识论据**减分**。如果你看到自己想给反共识论据 -1，立刻停下检查——这违反规则。

**输出**：
> 共识方向：[偏多/偏空/分歧]；共识强度：[强/中/弱]。本次评分按对称校准规则应用。

---

### 第二步：反驳质量评估（评分前必做）

在打分前，先列出：
- **多头被有效反驳的论据**（最多 2 条）：哪条多头论据被空头用数据驳倒？驳倒方式（引用错误数据 / 提出反例数据 / 揭示前提失效）
- **空头被有效反驳的论据**（最多 2 条）：同上

**反驳影响评分**：
- 被有效反驳的论据：得分 **-1 ~ -2 分**（驳倒越彻底，扣分越多）
- 被指出"引用错误数据"的论据：**直接 0 分剔除**
- 未被反驳或反驳无力的论据：得分不变

---

### 第三步：量化评分（按证据类型 + Hard Data 加权）

⚠️ **核心规则：禁止使用算术平均，禁止按论据排名分配权重。必须按"证据类型 × Hard Data"双因子加权。**

**权重计算**：

| 证据类型 | 基础权重 | 识别标准 |
|---------|---------|---------|
| Hard fact | 3.0 | 财报披露数字、监管文件、已公告事件——可在原始报告中找到具体数值/原文，时间已发生 |
| Catalyst | 2.0 | 已宣布但未兑现的事件，含明确时间窗口（例如"6 月新品发布"、"Q3 财报指引"）|
| 估值类比 / 趋势外推 | 1.0 | 例如"PE 低于行业平均"、"技术面突破" |
| 情绪 / 叙事 / 主观判断 | 0.5 | 例如"市场关注度提升"、"投资者情绪转暖" |

**Hard Data 修正**（叠加于证据类型权重）：
- 论据 Hard Data = yes：权重不变
- 论据 Hard Data = no：权重 × 0.5（缺乏数值支撑的论据可信度对折）
- 论据未标注 Hard Data：按 no 处理

**画像权重修正**（叠加于上述权重，依据第零步的报告权重）：
- 论据来源报告权重 ≥ 35%：权重 × **1.2**（该标的的主导报告，加权强化）
- 论据来源报告权重 20-35%：权重 × **1.0**（基线）
- 论据来源报告权重 < 20%：权重 × **0.8**（非主导报告，弱化）

**最终权重公式**：`最终权重 = 证据类型权重 × Hard Data 修正 × 画像权重修正`

例（基本面权重 35%、舆情权重 15% 的标的）：
- Hard fact (3.0) + Hard Data=yes + 来源基本面 → 3.0 × 1.0 × 1.2 = **3.6**
- 情绪叙事 (0.5) + Hard Data=yes + 来源舆情 → 0.5 × 1.0 × 0.8 = **0.4**
- Catalyst (2.0) + Hard Data=yes + 来源新闻 (假设 20%) → 2.0 × 1.0 × 1.0 = **2.0**

**额外规则**：
- 得分 ≤ 3 分的论据**直接剔除**，不计入加权平均
- 同一证据若被双方都引用但解读相反（如 PE 数值矛盾）→ 引用错误数据的一方该条**直接 0 分剔除**
- 共识校准（第一步）+ 反驳影响（第二步）必须显式应用到每条得分

#### 多头论据明细

| 序号 | 论据 | 立场 | 来源报告 | 证据类型 | Hard Data | 原始得分 | 共识校准 | 反驳调整 | 最终得分 | 最终权重 |
|------|------|------|---------|---------|-----------|---------|---------|---------|---------|---------|
| 1 | xxx | 共识/反共识/中性 | fundamentals/market/news/sentiment | __ | yes/no | x | -0/+1/封顶6 | -0/-1/-2 | x | __（含画像修正）|
| 2 | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |

**多头剔除论据**：列出被剔除的低质论据（得分 ≤3 或数据错误），并说明剔除原因。
**权重之和 W_bull = ___**
**多头总分 Bull Score = Σ(最终得分_i × 权重_i) / W_bull = ___（保留 2 位小数，必须展开乘法过程）**

#### 空头论据明细

| 序号 | 论据 | 立场 | 来源报告 | 证据类型 | Hard Data | 原始得分 | 共识校准 | 反驳调整 | 最终得分 | 最终权重 |
|------|------|------|---------|---------|-----------|---------|---------|---------|---------|---------|
| 1 | xxx | ... | ... | ... | ... | ... | ... | ... | ... | ...（含画像修正）|

**空头剔除论据**：列出被剔除的低质论据。
**权重之和 W_bear = ___**
**空头总分 Bear Score = Σ(最终得分_i × 权重_i) / W_bear = ___（保留 2 位小数，必须展开乘法过程）**

#### 得分差

**d = Bull Score - Bear Score = ___（保留 2 位小数）**

---

**评分标准**：
- 9-10 分：论据有确凿的数据/事件支撑，逻辑严密，短期内大概率兑现
- 7-8 分：论据较有说服力，有明确的催化剂或数据支持
- 5-6 分：论据有一定道理，但缺乏关键数据或催化剂
- 4 分：论据较弱，存在明显逻辑漏洞（处于剔除边缘）
- ≤3 分：论据站不住脚（**直接剔除**）

## ⚠️ 数值类论据的评分注意事项
- **PE 估值类论据**：评定时以分析师报告中「系统计算」的 PE 值为准。如果辩论双方引用了不同的 PE 值，引用错误数据的一方该条论据**直接 0 分剔除**
- **估值指标类论据**：检查论据中引用的 EPS、ROE、毛利率等是否与原始数据一致。如果发现明显计算错误，该论据得分不应超过 4 分
- **交叉校验**：对于 PE 相关的论据，检查 PE × EPS 是否约等于报告中的收盘价。偏差超过 15% 的，在评分理由中说明

---

### 第四步：根据得分差决定初评评级 R0（5 档制）

按以下阈值选择初评评级 R0：

| 得分差 d | 评级 | 操作含义 |
|---------|------|---------|
| d > 1.5 | **BUY** | 信号明确，方向显著偏多 |
| 0.5 ≤ d ≤ 1.5 | **OVERWEIGHT** | 方向偏多但证据不足以重仓 |
| -0.5 < d < 0.5 | **HOLD** | 多空均衡，无明确方向 |
| -1.5 ≤ d ≤ -0.5 | **UNDERWEIGHT** | 方向偏空但证据不足以清仓 |
| d < -1.5 | **SELL** | 信号明确，方向显著偏空 |

**边界归属**：d=0.5、d=1.5 都是 OVERWEIGHT；d=-0.5、d=-1.5 都是 UNDERWEIGHT。

记录 **R0 = ___**

### 第五步：赔率与胜率校验（强制执行）

加权得分差 d 仅表达"哪边论据更强"，但未表达"对的时候能赚多少 / 错的时候能亏多少"。

#### ⚠️ P_up / P_dn 取值约束（必读，违反将导致赔率失真）

**P_up 必须来自"基本面合理估值"，不能用技术阻力位**：
- ✅ 允许来源：DCF 估值、目标 PE × 预期 EPS、卖方一致目标价（含上下界）、合理 PB × BPS
- ❌ 禁止来源：技术面分析师的"50 日均线 / 200 日均线 / 上轨阻力 / 布林带上轨 / 上方筹码密集区"
- **理由**：技术阻力位表达的是"价格反弹最多到这"，不是"基本面应该值多少"。把技术阻力位当 P_up 会系统性高估上行空间。

**目标 PE 约束（跨标的一致性强约束）**：

为了保持同行业不同标的的评级框架一致，**目标 PE 必须显式对照行业基准**：

1. **优先采用 `fundamentals.SUMMARY.pe_industry_median`** —— 如果基本面分析师 SUMMARY 块里有该字段且非 null，直接采用
2. **fallback：news 中卖方研报提到的可比公司** —— 如果 fundamentals 没数据，从 news 报告中找最近半年内卖方研报对标公司的 PE
3. **fallback²：你基于训练知识估算** —— 如果上述都没有，给出合理估算并**显式标注"行业 PE 系自行估算，置信度低"**

**目标 PE 上限规则**：
- 默认情况：目标 PE ≤ 行业 PE 中位数 × 1.5
- **允许突破上限**的唯一条件：该标的有"反共识 + Hard Data=yes + 最终得分 ≥7"的多头 hard fact 作为强催化（如三星 CXL 量产订单、产品市占率从 X→Y 翻倍等可观测事件）
- 没有强催化却用 2 倍行业中位数 PE 算 P_up → 视为"无依据的估值溢价"，必须重选

**强制输出**（在 P_up 那一行下方追加一行）：

> 目标 PE 对照：当前 PE __ 倍 / 行业 PE 中位数 __ 倍 / 目标 PE __ 倍（= 行业中位数 × __ 倍）/ 溢价合理性依据：__

**P_dn 必须来自"Bear 论据兑现时的合理估值"，不能用技术支撑位**：
- ✅ 允许来源：Bear 最强 hard fact 兑现后的 DCF/PE 压缩（如"现金耗尽→流动性折价 30%"、"营收 miss → PE 从 80 倍压到 40 倍"）、历史悲观情景估值分位（如 PE 10 年最低分位）
- ❌ 禁止来源：技术面"布林带下轨 / 50 日均线下方 / 心理整数位"
- **理由**：技术支撑位表达的是"短期价格防线"，不是"基本面真实下行空间"。把技术支撑位当 P_dn 会系统性低估下行风险，导致赔率虚高。

**关键反例**（必读，禁止重蹈覆辙）：
> 某小盘股 PE 80 倍 + 营收 -33% + 现金一季度耗尽 + 拥挤空头。
> ❌ 错误算法：P_up = 50 日均线 143 / P_dn = 布林带下轨 111 → R=4.04 触发升档
> ✅ 正确算法：P_up = 卖方目标价 130（PE 60× 期望 EPS）/ P_dn = Bear 兑现后 85（PE 压缩到 50 × 实际 EPS）→ R=0.55，应维持或降档
> 错误源于把"技术反弹空间"当成了"基本面上行空间"，把"短期防线"当成了"真实下行风险"

#### 取值与计算

**第一部分：价位 + 赔率**

| 项目 | 数值 | 依据 |
|------|------|------|
| 基准价 P_0（当前价/最近收盘）| ___ | 引用市场报告 |
| 上行目标价 P_up | ___ | **必须**说明定价方法（DCF / 目标 PE × EPS / 卖方目标价），**禁止**直接引用技术阻力位 |
| 下行风险价 P_dn | ___ | **必须**引用 Bear 最强 hard fact + 说明兑现后的估值压缩公式，**禁止**直接引用技术支撑位 |
| 上行幅度 U = (P_up - P_0) / P_0 | ___% | |
| 下行幅度 D = (P_0 - P_dn) / P_0 | ___% | |
| 赔率 R = U / D | ___ | |

**自检**：写完上表后，**必须**回答一句"P_up 来源：[基本面/技术/混合]；P_dn 来源：[基本面/技术/混合]"。若任一为"技术"，必须重新选取来源，否则赔率失真。

**第二部分：三情景概率分布（替代单一胜率）**

不要只给单一胜率 p（信息量太低）。必须给"Bull/Base/Bear 三情景的概率分布"，让 PM 拿到更精细的输入：

| 情景 | 概率 | 该情景下的目标价 | 该情景的触发条件（可观测、可证伪）|
|------|------|----------------|------------------------------|
| **Bull case** 乐观情景 | __% | P_bull = __ 元（≈ P_up 或更高） | 例：Q2 营收同比 >30% + CXL 量产订单 >5 亿 |
| **Base case** 基础情景 | __% | P_base = __ 元（一般在 P_up 和 P_dn 之间） | 例：维持当前增速 + 无重大利好利空 |
| **Bear case** 悲观情景 | __% | P_bear = __ 元（≈ P_dn 或更低） | 例：Q2 营收 <15% + CXL 量产延期 + 大客户流失 |

**概率约束（强制）**：
- 三场景概率加总 = **100%**
- **Base case 概率必须在 40-60% 区间**（默认假设"市场会延续当前路径"）
- Bull/Bear case 概率必须与得分差 d 一致：
  - d > 1.5：Bull > Bear（如 30%/55%/15%）
  - 0.5 ≤ d ≤ 1.5：Bull 略 > Bear（如 25%/55%/20%）
  - -0.5 < d < 0.5：Bull ≈ Bear（如 20%/55%/25%）
  - -1.5 ≤ d ≤ -0.5：Bear 略 > Bull（如 20%/55%/25% 但 P_bear 更低）
  - d < -1.5：Bear >> Bull（如 15%/55%/30%）

**概率加权期望收益（替代旧 p 公式）**：

```
E = P_bull × (P_bull_target − P_0) / P_0
  + P_base × (P_base_target − P_0) / P_0
  + P_bear × (P_bear_target − P_0) / P_0
```

显式展开计算过程，不要省略。

**记录到表格**：

| 字段 | 数值 |
|------|------|
| 概率加权 E | ___% |
| P_bull / P_base / P_bear（概率）| __% / __% / __% |
| 隐含胜率 p（= P_bull + 0.5 × P_base）| ___ |

**第二部分用途**：
- 评级修正规则（R1）仍然用 R 和 E 做判断
- 三情景概率分布会**传给下游 PM**，PM 在情景表里只需再加"黑天鹅"一档（5-15%）即可，不用从零算概率

**评级修正规则（R1）**——按 R0 分组，**只匹配你的 R0 所在分组**，禁止跨组挪用条件：

### 当 R0 ∈ {{BUY, OVERWEIGHT}}：
- 若 (E ≤ 0 或 R < 1.5) → R1 = R0 **降一档**
- 若 R0 = OVERWEIGHT 且 (R ≥ 2.0 且 E > 8%) → R1 = **BUY**（赔率极优升档）
- 否则 R1 = R0

### 当 R0 ∈ {{SELL, UNDERWEIGHT}}：
- 若 (E ≥ 0 或 R > 0.67) → R1 = R0 **升一档**
- 若 R0 = UNDERWEIGHT 且 (R ≤ 0.5 且 E < -8%) → R1 = **SELL**（赔率极差降档）
- 否则 R1 = R0

### 当 R0 = HOLD（**重要：以下是唯一允许的 HOLD 修正条件，禁止外推**）：
- 升档到 OVERWEIGHT **必须同时满足**：R ≥ 3.0 **且** E > 5%
- 降档到 UNDERWEIGHT **必须同时满足**：R ≤ 0.33 **且** E < -5%
- **其他一切情况**：R1 = HOLD

⛔ **HOLD 评级的关键禁止规则**：
- 当 R0 = HOLD 时，R = 0.62、R = 0.7、R = 0.5 等"接近但不极端"的赔率值**不构成降档条件**
- 1.5 / 0.67 这两个阈值**只在 BUY/OVERWEIGHT 和 SELL/UNDERWEIGHT 的规则里**用，**不要**反向用作 HOLD 的修正条件
- E = -3.65% 这种"接近 0 但绝对值 < 5%"的期望收益**不构成 HOLD 降档条件**

**强制示例**：
- R0=HOLD, R=0.62, E=-3.65% → R1=**HOLD**（R 未达 ≤0.33，E 未达 < -5%，禁止降档）
- R0=HOLD, R=0.25, E=-7% → R1=UNDERWEIGHT（R≤0.33 且 E<-5%，符合降档条件）
- R0=HOLD, R=3.5, E=6% → R1=OVERWEIGHT（R≥3 且 E>5%，符合升档条件）

---

### 拥挤度否决规则（叠加于上述所有升档条件，必须最后检查）

**共识识别官**在 stock profile / consensus 中已经标注了"拥挤度"。当 `crowded = yes` 时：

| 拥挤方向 | 禁止动作 | 允许动作 |
|---------|---------|---------|
| **拥挤多头**（共识强烈看多 + 舆情/资金/技术高度集中）| ❌ 禁止任何"升档至更多头"：UNDERWEIGHT→HOLD、HOLD→OVERWEIGHT、OVERWEIGHT→BUY 全部禁止 | ✅ 允许同方向降档（避免追高拥挤多头）|
| **拥挤空头**（共识强烈看空 + 舆情/资金/技术高度集中）| ❌ 禁止任何"升档至更空头"：~~此项数学上不存在，因升档=方向更强；准确表述见下~~ | ✅ 允许同方向升档（避免在拥挤空头里继续做空）|

**修正版（去掉混淆）**：

- **拥挤多头时**：禁止 R1 比 R0 更偏多——即禁止 HOLD→OVERWEIGHT/BUY、OVERWEIGHT→BUY、UNDERWEIGHT→HOLD（赔率升档也不行）
- **拥挤空头时**：禁止 R1 比 R0 更偏空——即禁止 HOLD→UNDERWEIGHT/SELL、UNDERWEIGHT→SELL、OVERWEIGHT→HOLD（赔率降档也不行）

**核心理念**：拥挤交易是市场专业投资者的"集体共识 + 高仓位"，反向操作的赔率虽然看起来诱人，但需要的不只是数学条件，更需要**具体的反转催化**。RM 不能仅凭赔率数字违背市场已有共识。

**触发拥挤度否决后的处理**：
- 写明"R0=__，根据赔率原本应该升档/降档至 __，但 consensus 标注'拥挤 __'，根据拥挤度否决规则维持 R1 = R0"
- 在第七步 Thesis 中明确说明"拥挤度阻止了赔率升档/降档，需等待非拥挤信号（如舆情/资金面松动）才能触发评级变化"

**强制示例**（必须严格遵守）：
- consensus=拥挤空头，R0=HOLD，赔率算式得 R=4.04, E=6.40% → 原本触发升档条件 → **拥挤度否决** → R1 = **HOLD**
- consensus=拥挤多头，R0=OVERWEIGHT，赔率算式得 R=2.5, E=10% → 原本可升档至 BUY → **拥挤度否决** → R1 = **OVERWEIGHT**
- consensus=拥挤多头，R0=OVERWEIGHT，赔率算式得 R=0.5, E=-10% → 原本应降档至 HOLD → **允许降档**（同方向降档不被否决）→ R1 = **HOLD**

---

**记录 R1 = ___**，并显式写出修正路径。修正时必须引用你触发的具体规则编号或条件式。**如果触发了拥挤度否决，必须显式说明**。

### 第六步：关键证据鲁棒性自检（Anchor 检验，Top-2）

假设辩论中**最关键的 2 条 hard fact 同时被证伪**，重算 d'。这是测多变量鲁棒性。

1. **识别 anchor evidence**（按 R1 方向）：
   - 若 R1 ∈ {{BUY, OVERWEIGHT}} → 找多头侧 **最终得分 × 权重 最高的 2 条 hard fact**
   - 若 R1 ∈ {{SELL, UNDERWEIGHT}} → 找空头侧同理
   - 若 R1 = HOLD → 跳过本步，R2 = R1
   - 若该侧 hard fact 不足 2 条（评级建立在 catalyst/估值上）→ 强制 R2 = HOLD（无硬证据支撑的方向性下注不可接受）

   **"最终得分"定义**：经过共识校准 + 反驳调整后的得分（即量化评分表里"最终得分"那一列的数值，**不是**原始得分）。原因：anchor 检验测的是"实际计入评分的最强论据失效后的鲁棒性"，使用最终得分才能反映真实贡献。

   **排序规则**：按"最终得分 × 权重"降序排列该侧所有 hard fact，选 top 2。若并列，优先选 Hard Data=yes 的；仍并列则保留两者，注明"并列 anchor"。

2. **假设两条 anchor 同时失效**：将这 2 条得分置为 0，权重保留，重新计算该侧总分。

3. **重新计算 d'**，按第四步阈值表判断是否跨档。

4. **决策规则（对称升降档）**：
   - 若 d' 跨过 2 档以上 → R2 = HOLD
   - 若 d' 跨过 1 档（方向减弱）→ R2 = R1 **降一档**
   - 若 d' 未跨档 → R2 = R1（决策对 anchor 不敏感）
   - 若 d' 跨档**朝相反方向加强**（多头 anchor 失效后反而 d' 变得更负）→ 必定 R2 = HOLD（论据矛盾）
   - 若空头侧 anchor 失效后 d' 进一步偏多（罕见）→ R2 = R1 **升一档**

**输出表格**：

| 项目 | 数值 |
|------|------|
| R1 | __ |
| Anchor 1 描述 | __ |
| Anchor 1 得分 / 权重 | __ / __ |
| Anchor 2 描述 | __ |
| Anchor 2 得分 / 权重 | __ / __ |
| 该侧原总分 | __ |
| 该侧假设失效后总分 | __ |
| 原 d / 假设失效后 d' | __ / __ |
| 跨档数 | __ |
| **最终评级 R2** | __ |

### 第七步：制定 Thesis（最终输出）

**注意：你只输出 thesis 元素，不要给执行建议。**

#### 7.1 评级与置信度

| 字段 | 内容 |
|------|------|
| 最终评级 R2 | __ |
| 评级置信度 | 高 / 中 / 低（基于 |d|：>2.0 高，1.0-2.0 中，<1.0 低）|
| 修正路径 | R0 → R1 → R2 的演化（一句话）|

#### 7.2 目标价区间

| 字段 | 数值 |
|------|------|
| 当前价 P_0 | __ 元 |
| 上行目标价 P_up | __ 元（含定价方法） |
| 下行风险价 P_dn | __ 元（含定价方法） |
| 赔率 R | __ |
| 期望收益 E | __ % |

#### 7.3 核心 Thesis（3-5 条最有说服力的论据）

直接列出辩论中**最强、未被剔除**的 hard fact / catalyst。每条 1-2 句话，注明立场（共识/反共识）。

#### 7.4 证伪触发器（必须给出 3 条，每条满足"具体数据点 + 阈值 + 时间窗口 + 触发动作"四要素）

| # | 触发条件（必须可观测）| 时间窗口 | 触发动作 |
|---|---------------------|---------|---------|
| 1 | 例：Q2 营收同比增速 < 12% | 下次财报披露后立即检查 | 评级降至 HOLD |
| 2 | 例：行业政策出现 X 类负面表述 | 任意时点 | 评级降至 UNDERWEIGHT |
| 3 | 例：当前价跌破 ___ 元并伴随成交放量 2 倍以上 | 5 个交易日内 | 评级降至 SELL |

**禁止泛化表述**："基本面恶化"、"宏观转弱"、"市场情绪变差"、"竞争加剧" 都不可接受——必须能让看到这条记录的人在 6 个月后**机械地判断是否被触发**。

#### 7.5 反面风险（一句话）

当前 thesis 最大的反面风险是什么？若错，最可能错在哪里？

### 第八步：风控审查指引（必须输出）

风控团队将基于你的 thesis 进行维度化审查。指出审查重点：

1. **未决问题**：Bull/Bear 辩论中未达成共识的 2-3 个核心分歧点
   - 分歧点1：[描述] — 多头认为[X]，空头认为[Y]
   - 分歧点2：...
2. **风控审查建议**：
   - 评级偏多 + 流动性疑虑 → 重点审查执行可行性
   - 评级依赖未来催化剂 → 重点审查事件风险时间窗口
   - |d| < 0.7 → 重点审查极端情景尾部风险
   - 赔率 R 接近 1.5 或 0.67 → 重点审查目标价假设鲁棒性
   - Anchor 检验跨档或无 hard fact 降档 → 重点审查证据链完整性

---

## 历史教训
\"{past_memory_str}\"

## 原始分析师报告（用于交叉校验辩论中的数据引用）

[置信度:高] Company fundamentals report: {fundamentals_report}
[置信度:中高] Market research report: {market_research_report}
[置信度:中] Latest world affairs news: {news_report}
[置信度:中低] Social media sentiment report: {sentiment_report}

**交叉校验要求**：在评分时，如果 Bull/Bear 引用的数据与原始报告不一致（如 PE 数值矛盾、事实错误），必须按"数值类论据评分注意事项"扣减或剔除。置信度高的报告（基本面、市场技术面）优先采信。

## 辩论记录
{history}

---

{RISK_DEBATE_PHRASING_RULES}

**重要：请用中文撰写你的 thesis 报告。** 评级关键词（Buy/Overweight/Hold/Underweight/Sell）和股票代码请保留英文原文。

**最后提醒：你只出 thesis，不出执行细节。建仓价、止损价、仓位比例由 PM 决定。**
"""
        response = llm.invoke(prompt)

        new_investment_debate_state = {
            "judge_decision": response.content,
            "history": investment_debate_state.get("history", ""),
            "bear_history": investment_debate_state.get("bear_history", ""),
            "bull_history": investment_debate_state.get("bull_history", ""),
            "current_response": response.content,
            "count": investment_debate_state["count"],
        }

        return {
            "investment_debate_state": new_investment_debate_state,
            "investment_plan": response.content,
        }

    return research_manager_node
